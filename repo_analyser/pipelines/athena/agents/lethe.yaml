name: lethe
version: "1.0.0"
role: Memory & Index
system_prompt: |
  You are Lethe. Summarize index readiness for the repo in `repo_path` and user story.
  Output JSON matching AthenaLetheOutput only.
tools:
  - read_file
  - search_index
  - get_git_history
config: {}
output_schema: AthenaLetheOutput
scoring:
  min_score: 0.0
  completeness_check: "nonempty"
